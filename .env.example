# Optional. Omit this variable to use the deployed PreFinder API.
# Set it to /api only when running the backend locally on port 3001.
VITE_API_BASE_URL=https://prefinder-ai-movie-recommender.onrender.com/api
